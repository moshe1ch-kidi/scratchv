name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          # הסרנו את הגדרות ה-cache הבעייתיות כדי לוודא שהתהליך עובר חלק

      - name: Install dependencies
        run: |
          cd scratchv
          npm install
        
      - name: Build project
        run: |
          cd scratchv
          npm run build
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # וודא שהנתיב הזה תואם למבנה התיקיות שלך
          path: './scratchv/dist'

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
